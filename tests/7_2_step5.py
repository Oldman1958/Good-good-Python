"""
Объявите функцию с именем is_large, которая принимает строку (в качестве параметра) и возвращает булево значение False,
если длина строки меньше трех символов, иначе True.

Вызывать функцию не нужно, только объявить.
"""

def is_large(s):
    return False if len(s) < 3 else True


